# Keep the merged development stack writable without adding a second,
# order-dependent rpm-ostree overlay above it. systemd-sysext discards this
# overlay's writes when the extension is unmerged or the system is rebooted.
[SysExt]
Mutable=ephemeral
